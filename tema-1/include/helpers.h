/**
 * Calculeaza cel mai mare divizor comun dintre doua numere
 */
unsigned long long gcd(unsigned long long a, unsigned long long b);

/**
 * Functie folosita in apelarea qsort. Sorteaza elementele in ordine descrescatoare
 */
int compareDescending(const void *const a, const void *const b);
